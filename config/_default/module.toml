# Hugo Module Configuration
#
# This site uses a self-contained atomic design architecture with local ITCSS styling.
# No external Hugo modules are required or used.
#
# Theme structure:
# - 5 atoms → 21 molecules → 2 organisms → 34 sections
# - ITCSS Layer 7: Bootstrap-compatible utilities (local implementation)
# - All components self-contained in themes/andromeda-hugo/

[hugoVersion]
extended = true
min = "0.147.2"
